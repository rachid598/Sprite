/*
 * Numéro de version de l'application, lisible et cité dans l'onglet Synchro.
 *
 * À incrémenter à chaque publication : c'est le seul endroit à modifier.
 * L'empreinte du dépôt reste affichée en dessous — elle identifie le contenu
 * exact déployé, ce qu'un numéro à deux chiffres ne peut pas faire.
 */
export const VERSION_APP = '0.1';
